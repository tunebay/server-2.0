import { expect } from 'chai';
import {
  generateToken,
  getUserIdFromToken,
  setUserInfo,
  comparePassword
} from '../../lib/auth';

xdescribe('🔑 Auth', () => {
  beforeEach(() => {
  });

  describe('generateToken()', () => {
    it('generates a token for a given user', () => {

    });
  });
});
